[project]
name = "python-formatter-thing"
version = "0.1.0"
description = "JSON-driven Python formatting rule checker"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }

[project.scripts]
pyfmtcheck = "python_formatter_thing.cli:main"


